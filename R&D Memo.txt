페이드 인 아웃 효과를 적용하다가 발생한 문제
  -> 씬 전환시 페이드 인 아웃 시 그 씬에 존재하는 UIToolKit UI와 렌더링 순서를 내 마음대로 조절이 불가능했음
    -> 클로드에게 물어보니 UI Toolkit은 씬 간 렌더링 순서가 불안정한 문제가 있다고 함
      => 기존 Fade In Out 효과를 UIDocument에서 Canvas(uGUI)로 바꿔서 해결
