# UI Design Reference

이 문서는 게임 UI의 비주얼 스타일 가이드입니다. 새 화면/컴포넌트를 만들 때 이 규칙을 기준으로 삼습니다.

## 컨셉

**Fantasy / Adventure / Hyper-casual / Cartoon**

- 밝고 채도 높은 색감의 캐주얼 모바일 RPG 톤
- 두꺼운 검은 아웃라인 + 말랑한 버블 스타일 (참고: KayKit "Adventurers" 로고 타이포)
- 참고 레퍼런스: Casual RPG GUI Kit(하이퍼캐주얼 버튼/패널 구조), 캐릭터 선택 스포트라이트 레이아웃
- 캐릭터 에셋(KayKit Adventurers, 스타일라이즈드 로우폴리)과 톤이 어긋나지 않도록, 무겁고 사실적인 텍스처보다는 밝고 단순한 카툰 표현을 우선

## 색상

| 용도 | 색상 | 코드 |
|---|---|---|
| 배경 하늘 | 하늘색 | `#7ecbf0` |
| 배경 초원(중간) | 연두 | `#a8e0a0` / `#bfe8b0` |
| 배경 초원(하단) | 진한 초록 | `#8fd070` / `#a0d47a` |
| 아웃라인(공통) | 검정 | `#16202a` |
| 골드/CTA 버튼 | 골드 그라디언트 | `#ffd97a` → `#e8963c` |
| 골드 텍스트(버튼 위) | 진한 갈색 | `#5a2e0c` |
| 나무 텍스처 베이스 | 갈색 | `#9a6a34` → `#6a4520` |
| 밝은 텍스트(카드/배지 위) | 크림 | `#fff8e0` |
| 공격/위험 계열 | 빨강 | `#c9382a` |
| 회복/방어 계열 | 초록 | `#6fbf3f` / `#7ac95a` |
| 영입/특수 계열 | 보라 | `#9a5fd4` |
| 정보/속도 계열 | 파랑 | `#3a7ac9` |

카테고리(버프/회복/디버프/영입 등)는 항상 같은 색을 사용해 의미를 고정합니다. (공격=빨강, 회복=초록, 영입=보라, 속도/정보=파랑)

## 텍스트 스타일

- 모든 주요 텍스트(타이틀, 버튼 라벨, 카드 제목)에 검은 아웃라인 적용
  ```css
  -webkit-text-stroke: 3px #16202a;
  paint-order: stroke fill;
  ```
- 아웃라인 굵기는 텍스트 크기에 비례해서 조정 (타이틀 4~5px, 버튼/카드 2~3px)
- 타이틀급 강조 텍스트는 컬러 글로우 추가 가능
  ```css
  text-shadow: 0 0 14px rgba(95,224,232,0.7), 0 6px 0 #0e161e;
  ```
- 폰트는 통통하고 둥근 계열 (Trebuchet MS류로 임시 대체, 실제 적용 시 라운드 고딕/카툰 폰트로 교체 권장)
- 폰트 굵기는 최대한 두껍게 (font-weight: 900)

## 버튼 / 패널 구조

### 공통 규칙
- 테두리: 검은색(`#16202a`) 3~5px
- 그림자: `box-shadow: 0 Npx 0 #16202a` (베벨 느낌의 하드 섀도우, blur 없이)
- 모서리: `border-radius` 12~16px

### CTA 버튼 (주요 액션: 시작, 선택 확정 등)
- 골드 그라디언트 배경 (`#ffd97a` → `#e8963c`)
- 필요 시 나뭇결 텍스처 오버레이
- 모서리에 리벳(작은 원) 장식 추가 가능

### 정보 배너 / 팝업 프레임 (부가 정보: 스테이지 표시, 턴 표시 등)
- 나무 질감 텍스처 사용
  ```css
  background:
    repeating-linear-gradient(90deg, #8a5a2e 0px, #8a5a2e 2px, #7a4c24 2px, #7a4c24 4px),
    linear-gradient(180deg, #9a6a34, #6a4520);
  ```

### 카테고리 카드 (로그라이크 선택지 등)
- 카테고리 색상 그라디언트 배경 + 검은 테두리
- 아이콘은 원형 배지 안에 흰색 아이콘으로 표시

## 배경

- 기본 배경: 하늘색 → 연두 → 초록 세로 그라디언트 (야외 필드 느낌)
- 구름 장식: 흰색 반투명 타원 2~3개, 화면 상단에 배치
- 전투/실내 팝업 등 어두운 톤이 필요한 화면은 갈색 계열(`#5a3a24` → `#3a2414`) 배경 사용

## 아이콘

- Tabler outline 아이콘 사용 (`ti-sword`, `ti-shield`, `ti-heart`, `ti-users`, `ti-bolt` 등)
- 흰색/크림색으로 채우고, 원형 컬러 배지 안에 배치

## 적용 화면 예시

- **TitleScene**: 골드 아웃라인 타이틀 로고 + 나무 텍스처 CTA 버튼
- **SettingScene(캐릭터 선택)**: 원형 스포트라이트 초상 + 스탯바(아이콘 포함) + 하단 인디케이터 점 + 나무 텍스처 CTA
- **로그라이크 선택지 팝업**: 카테고리 색상 카드 3장, 나무 텍스처 상단 배너
- **BattleScene**: 상단 나무 텍스처 정보바(스테이지/턴), 하단 나무 텍스처 턴 순서 표시줄

## 참고 레퍼런스

- KayKit - Adventurers Character Pack (아트 스타일 기준)
- Casual RPG GUI Kit (버튼/패널 구조, 하이퍼캐주얼 톤)
- "ADVENTURERS" 로고 타이포그래피 (텍스트 아웃라인 + 글로우 스타일)
